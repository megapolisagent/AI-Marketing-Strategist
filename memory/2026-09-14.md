# 2026-09-14

## Что сделали

По итогам разбора архитектуры (внешняя сессия Claude Code, не сам CMO) внесены правки в устав и правила:

1. **Технический permission-гейт** — `.claude/settings.json`: запуск любого субагента (`Agent`-инструмент) теперь требует ручного подтверждения владельца на уровне среды, `disableBypassPermissionsMode: "disable"` — независимо от самооценки агента по осям Impact/Uncertainty/Complexity. До этого входной гейт (`CLAUDE.md`, §7) существовал только как текст, ничем технически не подкреплённый.
2. **Калибровка входного гейта** (`CLAUDE.md`, §7) — сужен порог: продолжение уже одобренного скоупа и внутренний анализ (ничего не уходит специалисту/наружу) не требуют раунда вопросов, только маркировку ПРЕДПОЛАГАЮ; полный `pre-task-check` обязателен только для высокой оси и Positioning/Brand Gate. Устранено дублирование между `pre-task-check` (п.1-2) и `outcome-gate`.
3. **Новое правило `.claude/rules/tool-preference.md`** — Exa (`mcp__exa__web_search_exa`/`web_fetch_exa`) приоритетнее `WebSearch`/`WebFetch` для веб-доступа везде, не только в одном шаге `competitive-brief` как было раньше. Причина в основе — подтверждённый инцидент: WebFetch давал пусто/домыслы на JS-рендеренных страницах, Exa отдавала полный контент.
4. **Сшивка гейта с точками реального действия** — в `competitive-brief/SKILL.md` и `telegram-research/SKILL.md` добавлен явный чекпойнт перед стартом Research Process/Метода; в `competitor-researcher.md` и `market-researcher.md` добавлено явное указание не расширять скоуп по своей инициативе и приоритет Exa. Причина: общее правило в `CLAUDE.md` не работало на практике, потому что конкретные процедуры, которые реально грузятся в момент действия, о нём не знали.

## Что сделали (продолжение — та же сессия, повторный проход «убрать лишнее»)

Полная сверка всех документов агента (`CLAUDE.md`, все `.claude/rules/*`, все 12 `.claude/skills/*/SKILL.md`, все 3 `.claude/agents/*`, `README.md`, `.claude/agents/reviewer_calibration_log.md`) на мёртвые ссылки, устаревшие статусы и дублирование:

- **`lead-magnets/SKILL.md`** — убраны 6 мёртвых «See X» на несуществующие донорские скиллы (`free-tools`, `popups`, `social`, `ads`) и 2 мёртвые ссылки на `references/*.md`, которых физически нет; заголовок обновлён с «не исправлено» на «устранено».
- **`cro-auditor/SKILL.md`** — секция Integration с 4 несуществующими скиллами сведена к одной рабочей строке (данные через `performance-report`).
- **`humanizer/SKILL.md`** — исправлено устаревшее «не подключено как активный Skill», хотя он уже используется как критерий приёмки в `delegation-protocol.md` и `CLAUDE.md` §5 (тот же класс расхождения, что раньше поймали в `lead-magnets`).
- **`performance-report/SKILL.md`** — убраны мёртвые `~~зачёркнутые~~` плейсхолдеры донора про несуществующие MCP-коннекторы аналитики, оставлена только рабочая ветка (владелец приносит цифры текстом).
- **`README.md`** — исправлено устаревшее число скиллов (11→12), добавлена папка `agents/` и `settings.json` в схему структуры.

Не тронуто намеренно: `knowledge/businesses/*` (это данные/исследование, не архитектура агента — численные коллизии в нумерации файлов 10_/11_ есть, но трогать содержательную базу знаний без отдельного запроса рискованно); все 9 `.claude/rules/*.md` — при повторной проверке не нашли дублирования, кроме уже устранённого в CLAUDE.md §7 в этой же сессии.

## Что сделали (продолжение — исправление на уровне FOUNDATION/ENGINEER)

Разбор ушёл на уровень выше собственного репозитория: Входной гейт §7 этого файла оказался копией текста `FOUNDATION/instructions/confirmation-before-action.md`, который при переносе слился с осевым гейтом `HOME.md` в самопротиворечие («без исключений по осям» + «оси низкие → сразу» в одном абзаце) — тот же баг найден и в `ENGINEER/CLAUDE.md` §3. Исправлено на источнике (`FOUNDATION/instructions/confirmation-before-action.md`, `HOME.md`, `DECISIONS.md`) и синхронизировано в `ENGINEER`; формулировка в этом файле сведена к единому канону (добавлена перекрёстная ссылка).

## Что сделали (продолжение — перенос защитных хуков от ENGINEER)

Консолидированы 4 файла правил (`ask-before-searching.md`, `direct-reading-policy.md`, `direct-response-policy.md`, `verify-before-analyze.md`) в один `data-discipline.md` — по образцу `ENGINEER/01-data-discipline.md`, ссылок на старые имена нигде не было, старые файлы удалены с подтверждения владельца. `.claude/rules/` теперь 7 файлов (было 10).

Перенесены и подключены в `.claude/settings.json` два хука от ENGINEER: `guard-rules-bloat.sh` (потолок 9 файлов в `.claude/rules/` + блокировка инцидент-нарративного контента в новых правилах) и `block-dangerous-git.sh` (блокирует `git reset --hard`/`clean -f`/force-push/`branch -D`). Оба протестированы pipe-тестом на синтетических payload — сработали как ожидалось.

## Что сделали (продолжение — сокращение ритуала без доказанной пользы, по прямому запросу владельца)

По итогам разбора «что реально держит от беды, а что просто ритуал» (внешняя сессия, 2026-09-14) — вырезано 4 пункта:
- `capability-resolver.md` — снят весь протокол внешней разведки (3 источника + Platform Decision Record): физически невыполним этим агентом, он не строит новые скиллы сам. Оставлена одна строка «назови потребность владельцу, дальше — Engineer».
- `memory-rules.md` — дневник теперь по факту (было решение/правка/результат), не по календарю на первом рубеже дня.
- `CLAUDE.md` §7 п.4 и `pre-task-check.md` — полный письменный чек-лист сужен до Positioning/Brand Approval Gate; для обычной высокой оси хватает брифа, который и так требует Confirmation Gate — не дублируется вторым письменным блоком.
- `marketing-strategy/SKILL.md`, шаги 2 и 8 — полный скилл `outcome-gate` подключается только для Positioning/Brand-уровня; для узкого вопроса по одной способности — одна строка разрыва, без вызова скилла.

Проверен и НЕ вырезан (изначально предполагалось как кандидат, не подтвердилось): формальный DECISIONS.md у AI Legal Constructor — при чтении оказался уже лёгким индексом (одна строка на решение, полный текст в архиве), не тяжёлым ритуалом — решение оставить как есть.

## Что сделали (продолжение — реальный кейс ALIA вскрыл третий повтор известного урока, встроен принудительный фикс)

По итогам разбора зависшей задачи ALIA (Авитолог, вторая карточка отдана 11 сентября, три дня без движения) нашли: карточка данными и брифом готова, ждёт только «да» владелицы — но никто не спросил статус три дня; отдельно — сравнение карточек с баннерной рекламой (запрошено 31 августа) вообще не подхватилось. Тот же урок («ты идёшь за мной, а не предлагаешь сама») уже ловился 31 августа и 12 сентября — третий повтор показал, что запись в `memory/feedback_*` не заставляет применить урок сама по себе.

Встроено принудительно, не третьей записью в память:
- **`CLAUDE.md` §3** — новый обязательный шаг «Проактивный статус-скан»: перед ответом по уже открытому делу сам проверяешь файлы статуса специалистов (`workspace/kpi-tracker.md` и т.п.) на зависшие пункты и называешь их первой строкой ответа, не дожидаясь вопроса. Пустой результат — тоже явная строка, не тишина.
- **`marketing-strategy/SKILL.md`, шаг 11** — явная отсылка на статус-скан из CLAUDE.md §3 для задач, ещё не вернувшихся от специалиста (шаг 11 сам по себе покрывал только уже полученный результат).
- Заодно почищена устаревшая ссылка на «Platform Decision Record» в §8 (протокол убран из `capability-resolver.md` ещё вчера, ссылка осталась висеть).

## Что осталось

- Проверить в реальной работе, что `permissions.ask` на `Agent` действительно всплывает подтверждением (может понадобиться перезапуск сессии/`/hooks`, чтобы среда подхватила новый `.claude/settings.json`).
- Понаблюдать, не стало ли различие «высокая/низкая ось» в §7 CLAUDE.md источником новых споров о том, к какому классу отнести конкретную задачу — если да, доуточнить признаки.

## Решения

Владелец одобрил все три направления правки одним запросом («вноси все исправления, о которых мы говорили») по итогам диагностики в отдельной сессии — калибровка входного гейта не отменяет правило от 2026-09-14, только сужает признаки срабатывания и добавляет техническое подкрепление вместо чисто текстового.
