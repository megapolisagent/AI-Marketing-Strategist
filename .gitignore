# Временные папки проекта
.obnovlenie-tmp/
.sklad-tmp/

# Скачанные сторонние репозитории для сверки роли (2026-08-27) — рабочий материал,
# не наш код, не коммитим целиком (лицензии, размер). Что взято из них — уходит в
# HOME.md с явной ссылкой на источник, не сюда.
research/marketing_reference/

# Переменные окружения и ключи
.env
.env.local
*.env
*.pem
*.key

# Локальные настройки и редакторы
.vscode/
.idea/
.claude/settings.local.json
.DS_Store
Thumbs.db

# Логи и временные файлы
*.log
*.tmp
*.bak

# Окружения и зависимости (Python / Node.js)
__pycache__/
*.py[cod]
.venv/
venv/
node_modules/
dist/
build/

# Локальные базы данных
*.sqlite
*.sqlite3
*.db
